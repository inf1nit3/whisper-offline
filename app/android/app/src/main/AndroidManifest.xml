<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--
          Auslöser für das Diktat. Bewusst exported, damit die Aktivität von außen
          startbar ist: Launcher-Shortcut, Tastenbeleger (Button Mapper u. ä.),
          Automatisierungs-Apps (Tasker, MacroDroid) und die Assistenten-Geste.

          ACTION_ASSIST macht die App in den Systemeinstellungen als Assistent
          wählbar — danach löst die zugehörige Geste bzw. Taste das Diktat aus
          (bei vielen Geräten langes Drücken der Ein-/Aus-Taste).

          Sicherheitsabwägung: von außen startbar heißt, jede App könnte den
          Dialog öffnen. Aufgenommen wird erst sichtbar im Vordergrund-Dialog,
          und ohne Zutun landet nichts irgendwo — der Text geht nur in die
          Zwischenablage, wenn der Nutzer "Fertig" antippt.
        -->
        <activity android:name=".DictationActivity"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Material.Light.Dialog.NoActionBar"
            android:label="@string/dictate_label"
            android:exported="true">
            <intent-filter>
                <action android:name="dev.whisper.transcribe.action.DICTATE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          Getrennter Launcher-Eintrag "Whisper Diktat". Damit taucht das Diktat in
          jeder App-Liste als eigenes Symbol auf und lässt sich von Tastenbelegern,
          die nur ganze Apps kennen, direkt zuweisen.
        -->
        <activity-alias
            android:name=".DictateLauncher"
            android:targetActivity=".DictationActivity"
            android:label="@string/dictate_label"
            android:icon="@drawable/ic_launcher"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service android:name=".DictationTileService"
            android:label="@string/dictate_label"
            android:icon="@drawable/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
